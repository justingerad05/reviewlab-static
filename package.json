{
  "name": "reviewlab-static",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx @11ty/eleventy && node generate-feed.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
